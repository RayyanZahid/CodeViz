---
phase: 14.1-fix-journey-build-and-start-completes-successfully-blocker
plan: 01
subsystem: infra
tags: [typescript, tsc-b, playwright, pnpm, build, tsconfig, project-references]

# Dependency graph
requires:
  - phase: 14-schema-foundation-and-shared-types
    provides: TypeScript source files in packages/server, packages/shared, packages/client
provides:
  - root pnpm build script using tsc -b project references across all three packages
  - composite: true in all three package tsconfigs enabling tsc -b participation
  - client tsconfig emitting to dist/ (noEmit removed, outDir: dist added)
  - Playwright config with webServer block auto-starting pnpm dev from project root
  - Journey test with correct pnpm build command, projectRoot cwd, dist verification, 4 tests
affects: [15.1-fix-journey-build-and-start-completes-successfully-blocker, all-phases-using-pnpm-build]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "tsc -b project references pattern: composite: true required in every tsconfig participating in build"
    - "Playwright webServer pattern: cwd must be project root when config lives in subdirectory"
    - "tsconfig.workers.json extends main tsconfig: override composite: false to prevent TS6304"

key-files:
  created: []
  modified:
    - package.json
    - packages/shared/tsconfig.json
    - packages/server/tsconfig.json
    - packages/client/tsconfig.json
    - packages/server/tsconfig.workers.json
    - .auto-gsd/journey-tests/playwright.config.ts
    - .auto-gsd/journey-tests/journey-build-and-start.spec.ts

key-decisions:
  - "root build script: pnpm build:workers && tsc -b packages/shared packages/server packages/client — workers must compile before tsc -b (server references worker CJS output)"
  - "client tsconfig Option A (modify existing): remove noEmit: true, add composite: true + outDir: dist — Vite ignores these options so dev workflow unaffected"
  - "shared tsconfig: add composite: true only, no outDir — shared exports point to .ts source directly; dist verification only needs server and client"
  - "tsconfig.workers.json: add composite: false to override inherited composite: true — prevents TS6304 (composite projects may not disable declaration emit)"
  - "Playwright webServer reuseExistingServer: true — safe for local dev (reuses running server) and CI (no server pre-running, starts fresh automatically)"

patterns-established:
  - "Pattern: When a tsconfig extends another with composite: true, override composite: false in the child if it needs declaration: false"
  - "Pattern: Playwright journey test files use fileURLToPath(import.meta.url) for ESM __dirname to compute projectRoot for cwd"

requirements-completed: [INFRA-01, INFRA-02]

# Metrics
duration: 7min
completed: 2026-03-17
---

# Phase 14.1 Plan 01: Fix Build and Start Journey Test Summary

**Root pnpm build script with tsc -b project references, client tsconfig emitting to dist/, and self-contained Playwright journey tests with webServer auto-start**

## Performance

- **Duration:** 7 min
- **Started:** 2026-03-17T01:39:05Z
- **Completed:** 2026-03-17T01:46:00Z
- **Tasks:** 2
- **Files modified:** 7

## Accomplishments
- Added `build` script to root package.json running `pnpm build:workers && tsc -b packages/shared packages/server packages/client`
- Fixed all three package tsconfigs to participate in `tsc -b` project references (`composite: true`)
- Client tsconfig now emits to `dist/` (removed `noEmit: true`, added `outDir: dist`) — enables dist folder verification
- Playwright config has `webServer` block that auto-starts `pnpm dev` from project root with 60s timeout
- Journey test updated: uses `pnpm build` (not `npm run build`), sets `cwd: projectRoot`, includes 4 test cases including new dist folder verification

## Task Commits

Each task was committed atomically:

1. **Task 1: Add root build script and fix tsconfigs for tsc -b** - `ec355ed` (feat)
2. **Task 2: Fix Playwright config and journey test for self-contained execution** - `9b503da` (feat)

**Plan metadata:** (docs commit — see final_commit below)

## Files Created/Modified
- `package.json` - Added `build` script: `pnpm build:workers && tsc -b packages/shared packages/server packages/client`
- `packages/shared/tsconfig.json` - Added `composite: true`
- `packages/server/tsconfig.json` - Added `composite: true`
- `packages/client/tsconfig.json` - Replaced `noEmit: true` with `composite: true` + `outDir: dist`
- `packages/server/tsconfig.workers.json` - Added `composite: false` to override inherited composite setting
- `.auto-gsd/journey-tests/playwright.config.ts` - Added ESM __dirname, projectRoot, webServer block
- `.auto-gsd/journey-tests/journey-build-and-start.spec.ts` - Fixed build command, added cwd, added dist verification test, 4 total tests

## Decisions Made
- Used Option A for client tsconfig (modify existing, not separate tsconfig.build.json) — Vite ignores `outDir`/`noEmit` so dev workflow is unaffected, simpler approach
- Shared package: `composite: true` only, no `outDir` — exports point to TypeScript source directly, dist verification only covers server and client
- `reuseExistingServer: true` in webServer — safe default for both local dev and CI

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed tsconfig.workers.json TS6304 error from inherited composite setting**
- **Found during:** Task 1 (build verification run)
- **Issue:** `packages/server/tsconfig.workers.json` extends `tsconfig.json` which now has `composite: true`. TypeScript error TS6304: "Composite projects may not disable declaration emit" — because workers tsconfig sets `declaration: false` which conflicts with `composite: true`
- **Fix:** Added `"composite": false` to `tsconfig.workers.json` compilerOptions to override the inherited `composite: true`
- **Files modified:** `packages/server/tsconfig.workers.json`
- **Verification:** `pnpm build` exits 0 after fix
- **Committed in:** `ec355ed` (part of Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - Bug)
**Impact on plan:** Auto-fix was necessary for correctness — the inherited composite setting caused the workers build to fail. Fix is minimal (one field override). No scope creep.

## Issues Encountered
- tsconfig.workers.json inherited `composite: true` from server's main tsconfig via `extends`, causing TS6304. Resolved by adding `composite: false` override in workers tsconfig.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `pnpm build` now works and produces TypeScript output in `packages/server/dist/` and `packages/client/dist/`
- Playwright journey tests are self-contained with `webServer` auto-start
- Journey test "Build and Start" suite has 4 tests ready to run
- Unblocks phase 14.2 (journey test for schema foundation and shared types)

---
*Phase: 14.1-fix-journey-build-and-start-completes-successfully-blocker*
*Completed: 2026-03-17*

## Self-Check: PASSED

All files verified present. Both task commits found in git log.
