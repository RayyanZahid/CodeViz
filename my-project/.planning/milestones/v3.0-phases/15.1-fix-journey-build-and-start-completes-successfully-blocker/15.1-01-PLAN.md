---
phase: 15.1-fix-journey-build-and-start-completes-successfully-blocker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .auto-gsd/journey-tests/playwright.config.ts
  - .auto-gsd/journey-tests/journey-build-and-start.spec.ts
  - .auto-gsd/journey-tests/journey-phase-14.spec.ts
  - .auto-gsd/journey-tests/journey-phase-15.spec.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "All 4 journey test files exist in .auto-gsd/journey-tests/ with correct content from git HEAD"
    - "playwright.config.ts contains webServer block with pnpm dev command and 120s timeout"
    - "journey-build-and-start.spec.ts uses pnpm build with 180s timeout and verifies dist folders"
    - "journey-phase-14.spec.ts uses page.request against SERVER_URL:3100 for diagnostic endpoints"
    - "journey-phase-15.spec.ts exists and tests /api/timeline, /api/snapshot/:id, /api/intents endpoints"
    - "All 12 journey tests pass (green) when run via Playwright"
  artifacts:
    - path: ".auto-gsd/journey-tests/playwright.config.ts"
      provides: "Playwright config with webServer block, ESM imports, projectRoot"
      contains: "webServer"
    - path: ".auto-gsd/journey-tests/journey-build-and-start.spec.ts"
      provides: "4 build-and-start tests with pnpm build, dist verification"
      contains: "pnpm build"
    - path: ".auto-gsd/journey-tests/journey-phase-14.spec.ts"
      provides: "4 Phase 14 tests with diagnostic endpoint assertions"
      contains: "page.request"
    - path: ".auto-gsd/journey-tests/journey-phase-15.spec.ts"
      provides: "4 Phase 15 tests with timeline/snapshot/intents assertions"
      contains: "/api/timeline"
  key_links:
    - from: "git HEAD"
      to: ".auto-gsd/journey-tests/"
      via: "git checkout HEAD -- restore command"
      pattern: "git.*checkout.*HEAD"
---

<objective>
Restore all 4 journey test files from git HEAD to fix the auto-gsd regression that overwrote them with stubs and deleted journey-phase-15.spec.ts.

Purpose: Auto-gsd regenerated journey test files with simplified stubs that lack the webServer block, ESM imports, pnpm commands, and real API assertions. This is the same class of regression fixed in Phase 14.3. The correct, previously-verified versions exist in git HEAD. Restoring them is the complete fix.

Output: All 12 journey tests passing (green) across 3 test files.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.1-fix-journey-build-and-start-completes-successfully-blocker/15.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore journey test files from git HEAD and verify all 12 tests pass</name>
  <files>
    .auto-gsd/journey-tests/playwright.config.ts
    .auto-gsd/journey-tests/journey-build-and-start.spec.ts
    .auto-gsd/journey-tests/journey-phase-14.spec.ts
    .auto-gsd/journey-tests/journey-phase-15.spec.ts
  </files>
  <action>
    Restore all 4 journey test files from git HEAD using the git checkout command. The git repo root is one directory above my-project/, so paths must use the my-project/ prefix.

    Run this restore command from inside my-project/:
    ```bash
    git -C ".." checkout HEAD -- \
      "my-project/.auto-gsd/journey-tests/playwright.config.ts" \
      "my-project/.auto-gsd/journey-tests/journey-build-and-start.spec.ts" \
      "my-project/.auto-gsd/journey-tests/journey-phase-14.spec.ts" \
      "my-project/.auto-gsd/journey-tests/journey-phase-15.spec.ts"
    ```

    After restore, verify correct content with grep checks:
    - `grep "webServer" .auto-gsd/journey-tests/playwright.config.ts` (must match)
    - `grep "timeout: 120" .auto-gsd/journey-tests/playwright.config.ts` (must match)
    - `grep "pnpm build" .auto-gsd/journey-tests/journey-build-and-start.spec.ts` (must match)
    - `grep "timeout: 180000" .auto-gsd/journey-tests/journey-build-and-start.spec.ts` (must match)
    - `grep "page.request" .auto-gsd/journey-tests/journey-phase-14.spec.ts` (must match)
    - `grep "SERVER_URL" .auto-gsd/journey-tests/journey-phase-14.spec.ts` (must match)
    - `grep "/api/timeline" .auto-gsd/journey-tests/journey-phase-15.spec.ts` (must match)
    - `test -f .auto-gsd/journey-tests/journey-phase-15.spec.ts` (must exist)

    Then run the full journey test suite:
    ```bash
    cd .auto-gsd/journey-tests && node node_modules/.bin/playwright test
    ```

    All 12 tests must pass (green). Expected: 4 build-and-start + 4 phase-14 + 4 phase-15 = 12 tests.

    IMPORTANT: Do NOT manually rewrite the files. The git HEAD versions are correct and were verified passing in Phase 14.3's UAT. Always use `git checkout HEAD --` for this class of auto-gsd regression.

    IMPORTANT: The git path prefix is `my-project/` because the repo root is one level above. Using paths without this prefix will cause "pathspec did not match any file" errors.
  </action>
  <verify>
    1. `git -C .. status my-project/.auto-gsd/journey-tests/` shows no modifications or deletions for the 4 files
    2. All 6 grep checks above return matches
    3. `cd .auto-gsd/journey-tests && node node_modules/.bin/playwright test` exits with code 0 and reports 12 passed, 0 failed, 0 skipped
  </verify>
  <done>
    All 4 journey test files restored to their git HEAD versions. playwright.config.ts has webServer block. journey-build-and-start.spec.ts uses pnpm build with 180s timeout. journey-phase-14.spec.ts uses page.request against SERVER_URL:3100. journey-phase-15.spec.ts exists and tests /api/timeline, /api/snapshot/:id, /api/intents. All 12 journey tests pass green.
  </done>
</task>

</tasks>

<verification>
1. `git -C .. status my-project/.auto-gsd/journey-tests/` — no modified/deleted journey test files
2. `grep "webServer" .auto-gsd/journey-tests/playwright.config.ts` — webServer block present
3. `test -f .auto-gsd/journey-tests/journey-phase-15.spec.ts` — deleted file restored
4. Journey test suite: 12 passed, 0 failed, 0 skipped
</verification>

<success_criteria>
All 12 journey tests (4 build-and-start + 4 phase-14 + 4 phase-15) pass green when run via `cd .auto-gsd/journey-tests && node node_modules/.bin/playwright test`. No test files show as modified or deleted in git status.
</success_criteria>

<output>
After completion, create `.planning/phases/15.1-fix-journey-build-and-start-completes-successfully-blocker/15.1-01-SUMMARY.md`
</output>
