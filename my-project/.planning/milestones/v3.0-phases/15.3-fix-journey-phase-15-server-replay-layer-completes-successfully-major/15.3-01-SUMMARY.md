---
phase: 15.3-fix-journey-phase-15-server-replay-layer-completes-successfully-major
plan: 01
subsystem: testing
tags: [playwright, journey-tests, api-testing, typescript, http-assertions]

# Dependency graph
requires:
  - phase: 15.2-fix-journey-phase-14-schema-foundation-and-shared-types-completes-successfully
    provides: working server with /api/timeline, /api/snapshot/:id, /api/intents endpoints and NODE_ENV=development in playwright config
  - phase: 15-server-replay-layer
    provides: SnapshotManager, IntentAnalyzer, timelinePlugin, snapshot/:id endpoint, /api/intents endpoint
provides:
  - 4 passing Phase 15 journey tests with deep HTTP API assertions
  - journey-phase-15.spec.ts with real /api/timeline, /api/snapshot/:id, /api/intents endpoint assertions
  - workers:1 playwright config preventing watch root race conditions
  - 12 total journey tests passing green
affects: [16-mode-state-machine, future-journey-test-phases]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - Playwright workers:1 for server-state-sharing tests — any tests that POST /api/watch must run sequentially
    - Node ID assertion using relative paths — Pipeline uses event.relativePath so node IDs are "a.ts" not absolute paths
    - Poll-until-snapshot pattern with 30s deadline and 1s interval for async snapshot pipeline
    - Temp dir isolation per test via fs.mkdtempSync + POST /api/watch + finally cleanup

key-files:
  created:
    - .auto-gsd/journey-tests/journey-phase-15.spec.ts
  modified:
    - .auto-gsd/journey-tests/playwright.config.ts
    - .auto-gsd/journey-tests/journey-build-and-start.spec.ts
    - .auto-gsd/journey-tests/journey-phase-14.spec.ts

key-decisions:
  - "workers:1 in playwright.config.ts — tests sharing server state via POST /api/watch cannot run in parallel; watch root is global mutable server state"
  - "Node IDs are relative paths ('a.ts') not absolute paths — Pipeline.handleBatch passes event.relativePath to parser, not absolute path; RESEARCH.md Finding 4 was incorrect"
  - "Position assertion weakened to typeof object && not null — layout_positions table empty until Phase 6 (RESEARCH.md Finding 1 confirmed correct)"
  - "startSnapshotId asserted as null or number — handles both valid states per RESEARCH.md Finding 2"

patterns-established:
  - "Pattern 1: Sequential test runner required when tests share server global state (watch root, current session)"
  - "Pattern 2: Investigate actual server response format before asserting node/edge IDs — relative vs absolute depends on Pipeline implementation"

requirements-completed: [INFRA-03]

# Metrics
duration: 6min
completed: 2026-03-17
---

# Phase 15.3 Plan 01: Fix Journey Phase 15 Server Replay Layer Tests Summary

**4 Phase 15 journey tests passing with deep HTTP assertions against /api/timeline, /api/snapshot/:id, and /api/intents endpoints — 12 total tests green**

## Performance

- **Duration:** 6 min
- **Started:** 2026-03-17T05:00:46Z
- **Completed:** 2026-03-17T05:07:03Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments
- Restored 3 regressed journey test infrastructure files (playwright.config.ts, journey-build-and-start.spec.ts, journey-phase-14.spec.ts) from git HEAD
- Wrote journey-phase-15.spec.ts with 4 real HTTP API tests per CONTEXT.md deep assertion spec
- Added workers:1 to playwright.config.ts to prevent watch root race conditions in parallel test execution
- All 12 journey tests pass green (4 build-and-start + 4 phase-14 + 4 phase-15)

## Task Commits

Each task was committed atomically:

1. **Task 1: Restore 3 regressed infrastructure files from git HEAD** - (no separate commit needed — files restored to HEAD state, Task 2 commit captures playwright.config.ts change)
2. **Task 2: Write journey-phase-15.spec.ts with deep CONTEXT.md-level assertions** - `e0632d2` (feat)

## Files Created/Modified
- `.auto-gsd/journey-tests/journey-phase-15.spec.ts` - Created: 4 real Phase 15 tests with HTTP assertions, temp dir isolation, polling, shape validation, error cases (404/400), epoch range, monotonic sequenceNumbers, concurrency verification
- `.auto-gsd/journey-tests/playwright.config.ts` - Modified: added workers:1 to prevent parallel watch root switching race conditions; restored from HEAD (webServer block, NODE_ENV=development, ESM projectRoot)
- `.auto-gsd/journey-tests/journey-build-and-start.spec.ts` - Restored from HEAD: pnpm build 180s timeout, cwd projectRoot, 4 tests including dist verification
- `.auto-gsd/journey-tests/journey-phase-14.spec.ts` - Restored from HEAD: real diagnostic endpoint assertions against SERVER_URL:3100

## Decisions Made
- workers:1 added to playwright config — discovered tests fail in parallel because POST /api/watch switches global server state; sequential execution ensures test isolation
- Node ID assertion uses relative paths ('a.ts') not absolute paths — Pipeline uses event.relativePath so node IDs are short names, not full absolute paths

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Corrected node ID assertion from absolute paths to relative paths**
- **Found during:** Task 2 (Write journey-phase-15.spec.ts)
- **Issue:** RESEARCH.md Finding 4 stated "node IDs are file-path IDs (e.g., /tmp/archlens-ph15-abc123/module0.js)" — but the actual server returns relative paths like "a.ts". Pipeline.handleBatch passes event.relativePath to the parser, not the absolute path.
- **Fix:** Changed assertion from `expect(nodeIds).toContain(fileA)` (absolute path) to `expect(nodeIds).toContain('a.ts')` (relative path)
- **Files modified:** .auto-gsd/journey-tests/journey-phase-15.spec.ts
- **Verification:** Test 2 passed after fix; nodeIds confirmed to contain 'a.ts' and 'b.ts'
- **Committed in:** e0632d2 (Task 2 commit)

**2. [Rule 1 - Bug] Added workers:1 to playwright.config.ts to fix parallel execution race condition**
- **Found during:** Task 2 verification (running all 12 tests together)
- **Issue:** When Playwright ran 3 workers in parallel, tests doing POST /api/watch interfered — one test's watch root switch caused another test's session to have 0 snapshots. Phase 15 snapshot test got 0 nodes; Phase 14 delta-threshold test got 0 snapshots.
- **Fix:** Added `workers: 1` to defineConfig in playwright.config.ts to enforce sequential execution
- **Files modified:** .auto-gsd/journey-tests/playwright.config.ts
- **Verification:** All 12 tests pass green with workers:1 (20.6s total runtime)
- **Committed in:** e0632d2 (Task 2 commit, alongside phase-15 test file)

---

**Total deviations:** 2 auto-fixed (2 Rule 1 bugs)
**Impact on plan:** Both fixes necessary for test correctness. No scope creep. The position assertion and startSnapshotId assertion were correctly implemented per RESEARCH.md critical findings (positions as `typeof object && not null`, startSnapshotId as `null || number`).

## Issues Encountered
- RESEARCH.md Finding 4 was incorrect about node ID format — investigation of Pipeline.ts source confirmed relative paths are used, not absolute paths. Corrected during test execution.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All 12 journey tests passing green with real HTTP API assertions
- Test suite is ready for Phase 16 (Mode State Machine) journey test additions
- workers:1 pattern is established — future tests that POST /api/watch must continue with sequential execution

---
*Phase: 15.3-fix-journey-phase-15-server-replay-layer-completes-successfully-major*
*Completed: 2026-03-17*
