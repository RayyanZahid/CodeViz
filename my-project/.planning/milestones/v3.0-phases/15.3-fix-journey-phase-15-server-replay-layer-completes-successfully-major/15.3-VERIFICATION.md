---
phase: 15.3-fix-journey-phase-15-server-replay-layer-completes-successfully-major
verified: 2026-03-17T05:13:04Z
status: human_needed
score: 5/6 must-haves verified
human_verification:
  - test: "Run all 12 journey tests end-to-end against a live dev server"
    expected: "12 tests pass green: 4 build-and-start, 4 phase-14, 4 phase-15. Zero failures."
    why_human: "Tests require a real running Fastify server on port 3100, SQLite persistence, chokidar file watcher, 3-second debounce, and Playwright browser. Cannot simulate full async pipeline in static analysis."
  - test: "Verify GET /api/snapshot/:id actually bounds reconstruction via checkpoints, not just stores full graphJson per snapshot"
    expected: "The O(50-max) performance claim from INFRA-03 is architecturally meaningful. Because every snapshot stores a full graphJson blob directly, the O(50-max) guarantee comes from FIFO pruning + checkpoint retention — not from iterative delta replay. Confirm this is the intended interpretation and the test name 'reconstructed from the nearest checkpoint in at most 50 replay steps' is accurate enough given the implementation."
    why_human: "The endpoint does snapshotsRepository.findById(numId) — a direct DB lookup on a pre-computed blob. No replay steps occur at read time. The '50 replay steps' is a capacity constraint on how many deltas can accumulate between checkpoints before FIFO pruning forces a checkpoint anchor. The test does not verify the O(50) bound; it verifies the endpoint response shape. A human must confirm whether the test name is a documentation mismatch or if a true delta-replay path was planned but not built."
---

# Phase 15.3: Fix Journey Phase 15 Server Replay Layer Tests - Verification Report

**Phase Goal:** Restore 3 regressed infrastructure files from git HEAD and write journey-phase-15.spec.ts with deep CONTEXT.md-level assertions (timeline shape + monotonic sequenceNumbers, snapshot node/edge/positions validation, IntentSession full shape, concurrency write+read, error cases 404/400) so all 12 journey tests pass green

**Verified:** 2026-03-17T05:13:04Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | GET /api/timeline returns snapshot metadata with id, sequenceNumber, timestamp, summary, triggerFiles after writing files and switching watch root | VERIFIED | journey-phase-15.spec.ts L9-60: full poll-until-snapshot loop, shape validation on each field, epoch range check, monotonic check |
| 2 | GET /api/snapshot/:id returns a complete graph with nodes matching written file paths, edges from real imports, and a positions object | VERIFIED | journey-phase-15.spec.ts L62-127: nodeIds assertion for 'a.ts'/'b.ts', edges.length > 0, positions typeof object && not null, 404/400 error cases |
| 3 | GET /api/snapshot/999999999 returns 404 and GET /api/snapshot/not-a-number returns 400 | VERIFIED | journey-phase-15.spec.ts L118-123: explicit status assertions for both error cases. timeline.ts L37-43 sends 400 for NaN, L43-44 sends 404 for missing |
| 4 | GET /api/intents returns at least one IntentSession with valid category, confidence 0-1, subtasks array, objective string, sessionId string | VERIFIED | journey-phase-15.spec.ts L129-193: unconditional sessions.length > 0, all 6 fields validated including validCategories array |
| 5 | GET /api/timeline responds within 5 seconds while files are being written concurrently, and new snapshots appear after the write burst | VERIFIED | journey-phase-15.spec.ts L196-251: 5000ms upper bound on responseTime, post-burst poll confirms pipeline alive |
| 6 | All 12 journey tests pass green (4 build-and-start + 4 phase-14 + 4 phase-15) | ? UNCERTAIN | Static count confirmed (4+4+4 = 12 tests). Requires live server execution to verify actual pass/fail. Commit e0632d2 references "20.6s total runtime" and "All 12 tests pass green" but cannot be verified without running the suite. |

**Score:** 5/6 truths verified (Truth 6 requires human execution)

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `.auto-gsd/journey-tests/playwright.config.ts` | webServer block with pnpm dev, 120s timeout, NODE_ENV=development, ESM projectRoot, workers:1 | VERIFIED | L1-28: all required fields present. workers:1 at L12, webServer.command='pnpm dev' at L19, timeout: 120*1000 at L21, NODE_ENV at L25, projectRoot ESM derivation at L5-6 |
| `.auto-gsd/journey-tests/journey-build-and-start.spec.ts` | 4 build/start tests with pnpm build 180s timeout and dist verification | VERIFIED | L11-17: execSync('pnpm build', timeout: 180000). L20-27: dist existence check. L30-43: dev server and JS errors. 4 tests confirmed. |
| `.auto-gsd/journey-tests/journey-phase-14.spec.ts` | 4 Phase 14 tests with real diagnostic endpoint assertions against SERVER_URL:3100 | VERIFIED | L3: SERVER_URL = process.env.SERVER_URL || 'http://localhost:3100'. 4 tests using page.request.get against /api/debug/... endpoints. |
| `.auto-gsd/journey-tests/journey-phase-15.spec.ts` | 4 Phase 15 tests with deep API assertions per CONTEXT.md decisions | VERIFIED | 253 lines. 4 tests in test.describe. All use temp dir isolation, SERVER_URL port 3100, finally cleanup. Deep shape/value assertions. |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `journey-phase-15.spec.ts` | `packages/server/src/plugins/timeline.ts` | HTTP requests to /api/timeline, /api/snapshot/:id, /api/intents | WIRED | 14 page.request calls found matching pattern `${SERVER_URL}/api/(timeline\|snapshot\|intents)`. All endpoints implemented in timeline.ts L15-77. |
| `journey-phase-15.spec.ts` | `packages/server/src/index.ts` | POST /api/watch to switch watch root and trigger pipeline | WIRED | 4 `page.request.post(\`${SERVER_URL}/api/watch\`)` calls (L18, L72, L145, L204). watchRootPlugin registered in index.ts L192-195. |
| `playwright.config.ts` | `packages/server/src/index.ts` | webServer block starts pnpm dev which launches Fastify on port 3100 | WIRED | playwright.config.ts L19: `command: 'pnpm dev'`. index.ts L221: `await fastify.listen({ port: 3100 })`. |
| `timeline.ts` | `packages/server/src/db/repository/snapshots.ts` | snapshotsRepository.getMetaBySession, findById | WIRED | timeline.ts L2 imports snapshotsRepository. L16: getMetaBySession(). L41: findById(). Repository exists and substantive (112 lines). |
| `timeline.ts` | `packages/server/src/db/repository/intentSessions.ts` | intentSessionsRepository.findBySession | WIRED | timeline.ts L3 imports intentSessionsRepository. L62: findBySession(). Repository exists and substantive (57 lines). |
| `index.ts` | `plugins/timeline.ts` | fastify.register(timelinePlugin, { getSessionId }) | WIRED | index.ts L115-117: registered with closure returning snapshotManager.getSessionId(). getSessionId() propagates correctly even after watch root switch (L173). |

---

## Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| INFRA-03 | 15.3-01-PLAN.md | Snapshot reconstruction uses checkpoints for O(50-max) performance | PARTIAL | The checkpoint infrastructure exists (snapshotCheckpoints table, checkpointsRepository, CHECKPOINT_INTERVAL=50 in SnapshotManager). However, GET /api/snapshot/:id does a direct snapshotsRepository.findById() — it retrieves a pre-computed graphJson blob in O(1), not an iterative delta replay bounded to 50 steps. The test name says "reconstructed from the nearest checkpoint in at most 50 replay steps" but no such replay path exists in code. The O(50) constraint is enforced via FIFO pruning at write time (checkpoints anchor snapshots, preventing pruning of the nearest full state), not via a read-time reconstruction algorithm. REQUIREMENTS.md marks INFRA-03 Complete. Whether this O(1) direct-lookup approach satisfies the spirit of INFRA-03 requires human judgment. |

**Orphaned requirements check:** No additional requirements mapped to Phase 15 in REQUIREMENTS.md beyond INFRA-03.

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `.auto-gsd/journey-tests/journey-phase-15.spec.ts` | 141 | `() => {}` inside string literal (test framework stub in test fixture data) | Info | Not a code stub — this is intentional test data content written to a temp file to create a spec pattern for IntentAnalyzer. No impact. |

No blocker or warning anti-patterns found. All 4 journey test files are substantive implementations:
- No `return null` / `return {}` / empty handlers
- No TODO/FIXME/placeholder comments
- All test bodies perform real assertions
- All HTTP calls use SERVER_URL (port 3100), not BASE_URL (port 5173) for API requests

---

## Human Verification Required

### 1. Full Journey Test Suite Execution

**Test:** From the project root, start the dev server (`pnpm dev`) and run all journey tests:
```
cd .auto-gsd/journey-tests && npx playwright test --reporter=list
```
**Expected:** 12 tests pass (4 build-and-start + 4 phase-14 + 4 phase-15), 0 failures, approximately 20-30 seconds total runtime with workers:1.

**Why human:** Requires a live Fastify server on port 3100, real chokidar file watching, SQLite writes, 3-second SnapshotManager debounce, and Playwright browser automation. The async pipeline (write files -> chokidar event -> delta -> snapshot) cannot be verified statically.

### 2. INFRA-03 Architectural Interpretation

**Test:** Read the GET /api/snapshot/:id implementation in `packages/server/src/plugins/timeline.ts` L34-57 alongside INFRA-03 ("Snapshot reconstruction uses checkpoints for O(50-max) performance").

**Expected:** Confirm that the implementation approach — storing full graphJson per snapshot, using checkpoints as FIFO pruning anchors (so at most 50 snapshots can be pruned between any two consecutive checkpoints) — satisfies the intent of INFRA-03. The test name "reconstructed from the nearest checkpoint in at most 50 replay steps" appears aspirational: the actual read path is O(1) direct lookup, not iterative reconstruction.

**Why human:** The correctness of INFRA-03 satisfaction depends on product intent. If O(50-max) was meant to describe the write-time architecture (checkpoint every 50 snapshots), the implementation is correct. If it was meant to describe a read-time delta-replay path, the endpoint does not implement that path and the requirement is only partially satisfied.

---

## Gaps Summary

No blocking gaps found for the observable test behaviors (truths 1-5). The artifacts are substantive and the key links are fully wired. The server-side infrastructure (SnapshotManager, IntentAnalyzer, timelinePlugin, watchRootPlugin) is complete and properly integrated.

The two human verification items are:
1. A runtime execution check (cannot be automated without running the server)
2. An architectural interpretation question about whether INFRA-03's O(50-max) language matches the chosen implementation design

Neither item is a code defect — they reflect the gap between static analysis and live test execution, plus a semantic ambiguity in the requirement.

---

_Verified: 2026-03-17T05:13:04Z_
_Verifier: Claude (gsd-verifier)_
