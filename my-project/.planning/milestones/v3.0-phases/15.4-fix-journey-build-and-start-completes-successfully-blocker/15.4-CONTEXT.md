# Phase 15.4: Fix Journey Build and Start completes successfully (blocker) - Context

**Gathered:** 2026-03-16
**Status:** Ready for planning

<domain>
## Phase Boundary

Restore 4 auto-GSD-overwritten journey test files from git HEAD and verify all 12 tests pass green. This is the 4th occurrence of the same regression pattern (files overwritten to placeholder content by auto-GSD orchestrator). Scope is strictly restore + verify + light investigation into the overwrite trigger.

</domain>

<decisions>
## Implementation Decisions

### Fix approach
- Restore all 4 files from git HEAD (both staged and unstaged changes reverted)
- Restore one file at a time with syntax verification after each, then full suite at end
- Full test run (pnpm exec playwright test) to prove all 12 tests pass green
- If any test fails after restore due to server changes, fix forward — update test to match current behavior, keeping assertion depth
- Generate fresh results.json via the full test run (replaces old one naturally)
- Commit messages follow established pattern: `fix(15.4-01): restore journey test files from git HEAD`
- Update ROADMAP.md phase goal from placeholder to specific intent

### Regression prevention
- No protection mechanism (read-only flags, checksums, etc.) — accept the pattern
- Document the recurring regression pattern in CONTEXT.md so future phases are aware
- Light investigation into what triggers auto-GSD to overwrite the files, findings noted but root cause fix is out of scope
- Future occurrences follow the same playbook: restore from git HEAD

### Build command alignment
- Use `pnpm build` (not npm) — matches the project's package manager
- Build timeout: 180 seconds (3 minutes) for full monorepo build (shared → server → client)
- Playwright auto-starts dev server via `webServer` config block (`pnpm dev`)
- Serial test execution with `workers: 1` to prevent shared server state race conditions

### Claude's Discretion
- Order of file restoration (which of the 4 files to restore first)
- Exact diff verification method after each file restore
- How deep to investigate the auto-GSD overwrite trigger

</decisions>

<specifics>
## Specific Ideas

- The committed versions were proven green at 2026-03-17T05:16:41Z (12 expected, 0 skipped, 0 unexpected per results.json)
- Committed playwright.config.ts includes: webServer block, workers: 1, path resolution via fileURLToPath
- Committed build-and-start test includes: pnpm build with 180s timeout, dist folder existence check, projectRoot resolution
- Committed phase-14 test uses diagnostic REST endpoints (GET /api/debug/graph-snapshots-table, /api/debug/intent-sessions-table, /api/debug/shared-types) with CRUD assertions
- Committed phase-15 test uses timeline/snapshot API endpoints with file-trigger workflows and deep shape assertions

### Recurring regression pattern (for future awareness)
This is the 4th time journey test files have been overwritten by auto-GSD orchestrator:
1. Phase 14.1 — first fix after Phase 14 execution
2. Phase 15.1 — second fix after Phase 15 execution
3. Phase 15.2/15.3 — third fix, split across Phase 14 and 15 tests
4. Phase 15.4 (this phase) — fourth fix, same overwrite pattern

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope

</deferred>

---

*Phase: 15.4-fix-journey-build-and-start-completes-successfully-blocker*
*Context gathered: 2026-03-16*
