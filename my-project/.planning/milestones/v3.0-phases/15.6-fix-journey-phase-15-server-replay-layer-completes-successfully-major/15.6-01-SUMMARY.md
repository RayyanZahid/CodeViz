---
phase: 15.6-fix-journey-phase-15-server-replay-layer-completes-successfully-major
plan: 01
subsystem: testing
tags: [playwright, journey-tests, canary, git-restore, auto-gsd]

# Dependency graph
requires:
  - phase: 15.5-fix-journey-phase-14-schema-foundation-and-shared-types-completes-successfully
    provides: restore-from-HEAD playbook confirmed; root cause documented (OttoGSD journey-test-generator.ts)
provides:
  - 4 journey test files restored from git HEAD with real assertions (SERVER_URL, workers:1, pnpm build)
  - journey-canary.spec.ts: new file that detects future auto-gsd placeholder overwrites via fs.readFileSync scanning
  - 13/13 Playwright journey tests passing green
affects: [all future phases — canary makes next auto-gsd overwrite event immediately visible as test failure]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Canary test: standalone Playwright test using Node.js fs to scan sibling specs for placeholder patterns"
    - "Overwrite detection: bodyText.length and page.goto-without-SERVER_URL as placeholder fingerprints"
    - "Restore playbook: git checkout HEAD -- <relative-path> from inside project directory handles staged/unstaged simultaneously"

key-files:
  created:
    - .auto-gsd/journey-tests/journey-canary.spec.ts
  modified:
    - .auto-gsd/journey-tests/playwright.config.ts (restored from HEAD)
    - .auto-gsd/journey-tests/journey-build-and-start.spec.ts (restored from HEAD)
    - .auto-gsd/journey-tests/journey-phase-14.spec.ts (restored from HEAD)
    - .auto-gsd/journey-tests/journey-phase-15.spec.ts (restored from HEAD)
    - .auto-gsd/journey-tests/results.json (updated by test run)

key-decisions:
  - "journey-canary.spec.ts uses async () (no page parameter) — no browser needed, avoids launch overhead"
  - "Canary exempts journey-build-and-start.spec.ts from page.goto-without-API check — it legitimately uses BASE_URL browser navigation"
  - "bodyText.length detection applies to ALL spec files including build-and-start (Pattern 1 universal)"
  - "Canary itself will be overwritten by auto-gsd (it is named journey-canary.spec.ts) — accepted; value is making next overwrite immediately visible"
  - "Restore from HEAD (not specific commit hash) — HEAD already contains correct versions for all 4 files"

patterns-established:
  - "Pattern: Canary spec file scans all sibling *.spec.ts files and fails test if placeholder patterns detected"
  - "Pattern: bodyText.length is the canonical auto-gsd placeholder fingerprint for detection"

requirements-completed: []

# Metrics
duration: 7min
completed: 2026-03-17
---

# Phase 15.6 Plan 01: Fix Journey Phase 15 Server Replay Layer Summary

**4 auto-gsd-overwritten journey test files restored from git HEAD plus new journey-canary.spec.ts that detects future placeholder overwrites, with 13/13 Playwright tests passing green**

## Performance

- **Duration:** ~7 min
- **Started:** 2026-03-17T06:50:06Z
- **Completed:** 2026-03-17T06:57:00Z
- **Tasks:** 2
- **Files modified:** 6

## Accomplishments

- Restored playwright.config.ts, journey-build-and-start.spec.ts, journey-phase-14.spec.ts, and journey-phase-15.spec.ts from git HEAD — all 4 contained correct real assertions (SERVER_URL, workers:1, pnpm build, diagnostic endpoint checks)
- Created journey-canary.spec.ts: a new Playwright test that reads all sibling spec files and fails immediately if bodyText.length placeholder patterns or page.goto-without-API-calls patterns are detected
- Ran full Playwright suite: 13/13 tests passed (4 build-and-start + 4 phase-14 + 4 phase-15 + 1 canary) in 20.8s

## Task Commits

Each task was committed atomically:

1. **Task 1: Restore 4 journey test files from git HEAD and create canary spec** - `610c07a` (fix) — journey-canary.spec.ts created; 4 restored files matched HEAD so no delta
2. **Task 2: Run full Playwright suite and verify 13/13 tests pass** - included in `610c07a` (results.json updated)

**Plan metadata:** committed in docs commit below

## Files Created/Modified

- `.auto-gsd/journey-tests/journey-canary.spec.ts` - New canary test: scans sibling specs for bodyText.length and page.goto-without-SERVER_URL placeholder patterns; fails test on any violation
- `.auto-gsd/journey-tests/playwright.config.ts` - Restored from HEAD: workers:1, webServer with NODE_ENV=development, 120s timeout, reuseExistingServer:true
- `.auto-gsd/journey-tests/journey-build-and-start.spec.ts` - Restored from HEAD: 4 tests (pnpm build, dist folder check, dev server responds, no JS errors)
- `.auto-gsd/journey-tests/journey-phase-14.spec.ts` - Restored from HEAD: 4 tests with real diagnostic endpoint assertions (SERVER_URL port 3100)
- `.auto-gsd/journey-tests/journey-phase-15.spec.ts` - Restored from HEAD: 4 tests (timeline, snapshot, intent, concurrent read/write)
- `.auto-gsd/journey-tests/results.json` - Updated by 13-test Playwright run (13 passed, 0 failed)

## Decisions Made

- Used `git checkout HEAD --` (from inside my-project/) for all 4 restorations — HEAD already contained correct versions for all files, no need for specific commit hashes
- Canary uses `async ()` (no `page` parameter) since it only reads files via Node.js fs — avoids any browser launch overhead
- journey-build-and-start.spec.ts is exempt from Pattern 2 check (page.goto without SERVER_URL) — it legitimately uses BASE_URL for Vite browser navigation tests, not the API server
- Canary will be overwritten by auto-gsd on the next phase completion — accepted by design; its value is making the next overwrite event immediately visible as test failure rather than a silent regression

## Deviations from Plan

None — plan executed exactly as written. The 4 files were overwritten stubs as expected; git HEAD contained the correct versions; `git checkout HEAD --` restored them cleanly; canary was created per spec; 13 tests passed green.

## Issues Encountered

The `gsd-tools.cjs commit` command failed with path resolution errors when the commit message contained spaces and the `--files` argument was included — the tool's argument parser split the commit message tokens as file paths. Resolved by using `git add` + `git commit -m` directly with a HEREDOC.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- All 13 journey tests are green and represent the complete Phase 15 exit criterion
- The canary test (journey-canary.spec.ts) will make the next auto-gsd overwrite event immediately visible as a test failure
- Phase 16 (Mode State Machine) can proceed — journey test infrastructure is verified stable

---
*Phase: 15.6-fix-journey-phase-15-server-replay-layer-completes-successfully-major*
*Completed: 2026-03-17*
