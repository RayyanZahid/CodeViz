---
phase: 14.1-fix-journey-build-and-start-completes-successfully-blocker
plan: 02
type: execute
wave: 1
depends_on: ["14.1-01"]
files_modified:
  - package.json
autonomous: true
requirements: [INFRA-01, INFRA-02]
gap_closure: true

must_haves:
  truths:
    - "Running the journey test suite does not fail with ERR_MODULE_NOT_FOUND for @playwright/test"
    - "All 4 Playwright journey tests in journey-build-and-start.spec.ts pass"
    - "Browser binaries are installed and available for headless Playwright runs"
  artifacts:
    - path: "package.json"
      provides: "@playwright/test in devDependencies"
      contains: "@playwright/test"
  key_links:
    - from: ".auto-gsd/journey-tests/playwright.config.ts"
      to: "@playwright/test"
      via: "import { defineConfig } from '@playwright/test'"
      pattern: "@playwright/test"
    - from: ".auto-gsd/journey-tests/journey-build-and-start.spec.ts"
      to: "@playwright/test"
      via: "import { test, expect } from '@playwright/test'"
      pattern: "@playwright/test"
---

<objective>
Install @playwright/test so the journey test suite can resolve its import and the 4 "Build and Start" tests pass end-to-end.

Purpose: Phase 14.1 plan 01 created the Playwright config and spec files but never added the @playwright/test package. The UAT confirmed the single remaining gap: ERR_MODULE_NOT_FOUND at runtime because the package is absent from root devDependencies.
Output: @playwright/test added to root package.json devDependencies, pnpm install completed, Playwright browser binaries installed, all 4 journey tests pass.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14.1-fix-journey-build-and-start-completes-successfully-blocker/14.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add @playwright/test to root devDependencies and install browser binaries</name>
  <files>package.json</files>
  <action>
    Add "@playwright/test": "^1.50.0" to the "devDependencies" object in the root package.json.

    The current devDependencies block is:
    ```json
    "devDependencies": {
      "concurrently": "^9.0.0",
      "typescript": "^5.8.0"
    }
    ```

    After the edit it should be:
    ```json
    "devDependencies": {
      "concurrently": "^9.0.0",
      "typescript": "^5.8.0",
      "@playwright/test": "^1.50.0"
    }
    ```

    Then run the following commands in sequence from the project root:
    1. `pnpm install` — installs @playwright/test into node_modules
    2. `pnpm exec playwright install --with-deps chromium` — installs the Chromium browser binary required for headless tests (only chromium, not all browsers, to minimize download size)

    Do NOT use `npx` — the project uses pnpm. Use `pnpm exec playwright install`.
    Do NOT install @playwright/test inside packages/server or packages/client — it belongs only at the root (root devDependencies, runs journey tests from .auto-gsd/journey-tests/).
  </action>
  <verify>
    1. `node -e "require('@playwright/test')"` (or `node --input-type=module -e "import('@playwright/test')"`) does not throw ERR_MODULE_NOT_FOUND
    2. `pnpm exec playwright --version` prints a version string (confirms binary is available)
    3. `cat package.json | grep playwright` shows "@playwright/test" in the output
  </verify>
  <done>
    @playwright/test is resolvable from the project root, browser binary is installed, and package.json devDependencies contains "@playwright/test".
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the journey test suite and confirm all 4 tests pass</name>
  <files></files>
  <action>
    Run the Playwright journey test from the .auto-gsd/journey-tests/ directory using:

    ```
    pnpm exec playwright test --config=.auto-gsd/journey-tests/playwright.config.ts
    ```

    Run from the project root. The webServer block in playwright.config.ts will auto-start `pnpm dev` (with 60s timeout, reuseExistingServer: true) before the tests run.

    Expected: 4 tests pass —
    - "App builds without errors" (runs pnpm build, expects exit 0)
    - "Build outputs exist in dist folders" (checks packages/server/dist and packages/client/dist exist with .js files)
    - "Dev server starts and responds" (page.goto returns status < 400)
    - "No JavaScript errors on load" (page.on('pageerror') captures no errors)

    If the webServer times out: check whether port 5173 is already bound. The reuseExistingServer: true setting will reuse a running server, so this is expected behavior on a dev machine.

    If any test fails for a reason OTHER than the @playwright/test missing package (e.g., a server crash, a page error), diagnose and fix the root cause. Do not mark this task done if tests fail.
  </action>
  <verify>
    `pnpm exec playwright test --config=.auto-gsd/journey-tests/playwright.config.ts` exits with code 0 and reports "4 passed".
  </verify>
  <done>
    All 4 tests in the "Build and Start" suite pass. Exit code is 0.
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `cat package.json` shows "@playwright/test" in devDependencies
2. `pnpm exec playwright --version` prints version without error
3. `pnpm exec playwright test --config=.auto-gsd/journey-tests/playwright.config.ts` exits 0, "4 passed"
</verification>

<success_criteria>
- @playwright/test is in root package.json devDependencies
- `pnpm install` has been run so the package is resolvable
- Playwright browser binaries (chromium) are installed
- All 4 journey tests in journey-build-and-start.spec.ts pass with exit code 0
- ERR_MODULE_NOT_FOUND for @playwright/test no longer occurs
</success_criteria>

<output>
After completion, create `.planning/phases/14.1-fix-journey-build-and-start-completes-successfully-blocker/14.1-02-SUMMARY.md`
</output>
