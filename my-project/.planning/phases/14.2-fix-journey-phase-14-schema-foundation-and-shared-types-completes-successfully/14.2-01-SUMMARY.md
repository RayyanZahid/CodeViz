---
phase: 14.2-fix-journey-phase-14-schema-foundation-and-shared-types-completes-successfully
plan: 01
subsystem: testing
tags: [playwright, fastify, drizzle-orm, sqlite, diagnostic, journey-tests]

# Dependency graph
requires:
  - phase: 14-schema-foundation-and-shared-types
    provides: graphSnapshots table, intentSessions table, SnapshotManager with delta-threshold logic, shared types in @archlens/shared/types
  - phase: 14.1-fix-journey-build-and-start-completes-successfully-blocker
    provides: Playwright webServer config, root build script, tsconfig composite setup
provides:
  - diagnosticPlugin with GET /api/debug/graph-snapshots-table, /api/debug/intent-sessions-table, /api/debug/shared-types
  - Rewritten journey-phase-14.spec.ts with 4 real tests calling server diagnostic endpoints
  - NODE_ENV-guarded diagnostic plugin registration in index.ts
affects: [15.2-fix-journey-phase-14-schema-foundation-and-shared-types-completes-successfully, journey test infrastructure]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Diagnostic REST endpoints (FastifyPluginAsync) for journey test verification — NODE_ENV guard, CRUD proof with cleanup, no test residue"
    - "page.request.get/post against SERVER_URL (port 3100) pattern for Playwright tests targeting Fastify directly (not Vite proxy)"
    - "Delta-threshold snapshot test: create temp dir, write N JS files, POST /api/watch to switch watcher, poll /api/timeline, assert snapshot_count < file_count"

key-files:
  created:
    - packages/server/src/plugins/diagnostic.ts
  modified:
    - packages/server/src/index.ts
    - .auto-gsd/journey-tests/journey-phase-14.spec.ts

key-decisions:
  - "diagnosticPlugin uses direct Drizzle calls (no repository abstraction) — diagnostic code doesn't warrant abstraction layer"
  - "Test 4 (delta-threshold) writes files before POSTing to /api/watch — files exist in tmpDir when chokidar starts watching, initial scan triggers delta events"
  - "All 4 Phase 14 tests call http://localhost:3100 (Fastify server), not http://localhost:5173 (Vite client) — explicit SERVER_URL constant prevents port confusion"

patterns-established:
  - "Pattern: Diagnostic plugin per journey test phase — thin wrapper over existing db/repo layer, auto-cleanup within request"
  - "Pattern: Journey tests use page.request for REST API calls, not page.goto + innerText"

requirements-completed: []

# Metrics
duration: 2min
completed: 2026-03-17
---

# Phase 14.2 Plan 01: Diagnostic Plugin and Journey Test Rewrite Summary

**Fastify diagnosticPlugin with 3 CRUD-proof endpoints + 4 real Playwright journey tests replacing body.length placeholder checks**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-17T02:15:24Z
- **Completed:** 2026-03-17T02:17:27Z
- **Tasks:** 2
- **Files modified:** 3 source files + compiled dist files

## Accomplishments

- Created `packages/server/src/plugins/diagnostic.ts` with 3 GET endpoints: `/api/debug/graph-snapshots-table` (CRUD proof with graphJson positions round-trip verification), `/api/debug/intent-sessions-table` (CRUD proof), `/api/debug/shared-types` (compilation proof via IntentCategory runtime check)
- Registered `diagnosticPlugin` conditionally in `index.ts` when `NODE_ENV !== 'production'` — endpoints available during `pnpm dev` tests, disabled in production
- Rewrote `.auto-gsd/journey-tests/journey-phase-14.spec.ts` with 4 real Playwright tests targeting Fastify server on port 3100 via `page.request`, eliminating the placeholder `page.goto + body.length > 0` pattern
- All TypeScript compiles successfully with `pnpm build`

## Task Commits

Each task was committed atomically:

1. **Task 1: Create diagnostic plugin and register conditionally in index.ts** - `559411d` (feat)
2. **Task 2: Rewrite journey-phase-14.spec.ts with real diagnostic endpoint tests** - `fbef76a` (feat)

## Files Created/Modified

- `packages/server/src/plugins/diagnostic.ts` - New diagnosticPlugin with 3 GET endpoints performing real CRUD operations on graph_snapshots and intent_sessions tables
- `packages/server/src/index.ts` - Added diagnosticPlugin import and conditional registration (NODE_ENV !== 'production')
- `.auto-gsd/journey-tests/journey-phase-14.spec.ts` - Rewritten with 4 real tests: CRUD proofs for both tables, shared-types compilation proof, delta-threshold behavioral test

## Decisions Made

- Direct Drizzle ORM calls inside plugin (no repository abstraction) — diagnostic endpoints are test-only utilities, not production code paths
- Files written to tmpDir before `POST /api/watch` in delta-threshold test — when chokidar starts watching the temp dir, existing files trigger initial scan delta events (structural `addedNodes`)
- `SERVER_URL` constant in journey test defaults to `http://localhost:3100` — explicit port prevents accidental calls to Vite client at port 5173

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- Phase 14.2 diagnostic endpoints are ready; journey test runner can now validate all 4 Phase 14 success criteria
- The delta-threshold test requires the SnapshotManager to process file writes against a temp dir — depends on pipeline scan + 3s debounce completing within the 30s poll window
- Phase 15.2 journey fix work can reference this pattern for its own diagnostic endpoints

---
*Phase: 14.2-fix-journey-phase-14-schema-foundation-and-shared-types-completes-successfully*
*Completed: 2026-03-17*

## Self-Check: PASSED

- packages/server/src/plugins/diagnostic.ts: FOUND
- packages/server/src/index.ts: FOUND
- .auto-gsd/journey-tests/journey-phase-14.spec.ts: FOUND
- Commit 559411d: FOUND
- Commit fbef76a: FOUND
