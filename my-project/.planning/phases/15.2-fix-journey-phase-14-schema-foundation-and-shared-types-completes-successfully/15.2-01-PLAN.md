---
phase: 15.2-fix-journey-phase-14-schema-foundation-and-shared-types-completes-successfully
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .auto-gsd/journey-tests/playwright.config.ts
  - .auto-gsd/journey-tests/journey-build-and-start.spec.ts
  - .auto-gsd/journey-tests/journey-phase-14.spec.ts
  - .auto-gsd/journey-tests/journey-phase-15.spec.ts
autonomous: true
requirements: [INFRA-01, INFRA-02]

must_haves:
  truths:
    - "All 4 Phase 14 journey tests pass with real diagnostic endpoint assertions (not placeholder body-length checks)"
    - "All 4 Phase 15 journey tests pass unchanged from prior working state"
    - "All 4 Build-and-Start journey tests pass unchanged from prior working state"
    - "Delta-threshold test uses 15-20 file writes and a fixed 5s wait per CONTEXT.md decisions"
    - "playwright.config.ts explicitly sets NODE_ENV=development in webServer env block"
  artifacts:
    - path: ".auto-gsd/journey-tests/playwright.config.ts"
      provides: "Playwright config with webServer block, ESM imports, NODE_ENV=development"
      contains: "NODE_ENV"
    - path: ".auto-gsd/journey-tests/journey-build-and-start.spec.ts"
      provides: "Build-and-start tests with pnpm build, 180s timeout, dist verification"
      contains: "pnpm build"
    - path: ".auto-gsd/journey-tests/journey-phase-14.spec.ts"
      provides: "Phase 14 tests: graph_snapshots CRUD, intent_sessions CRUD, shared types compilation, delta-threshold"
      contains: "SERVER_URL"
    - path: ".auto-gsd/journey-tests/journey-phase-15.spec.ts"
      provides: "Phase 15 tests: timeline, snapshot reconstruction, intent classification, concurrent pipeline"
      contains: "SERVER_URL"
  key_links:
    - from: ".auto-gsd/journey-tests/journey-phase-14.spec.ts"
      to: "packages/server/src/plugins/diagnostic.ts"
      via: "HTTP GET to /api/debug/* endpoints"
      pattern: "api/debug/graph-snapshots-table|api/debug/intent-sessions-table|api/debug/shared-types"
    - from: ".auto-gsd/journey-tests/journey-phase-14.spec.ts"
      to: "packages/server/src/plugins/timeline.ts"
      via: "HTTP GET to /api/timeline for delta-threshold verification"
      pattern: "api/timeline"
    - from: ".auto-gsd/journey-tests/playwright.config.ts"
      to: "packages/server/src/index.ts"
      via: "webServer block starts pnpm dev which launches Fastify on port 3100"
      pattern: "pnpm dev"
---

<objective>
Restore 3 regressed journey test infrastructure files from git HEAD (with NODE_ENV=development addition to playwright.config.ts) and rewrite journey-phase-14.spec.ts fresh per CONTEXT.md locked decisions (15-20 files, fixed 5s wait, descriptive failure messages, hasIntentCategory check).

Purpose: Auto-gsd has overwritten all 4 journey test files with simplified stubs after Phase 15.1 completed. The Phase 14 tests need to be rewritten with updated decisions from CONTEXT.md (not simply restored from HEAD).
Output: All 12 journey tests (4 build-and-start + 4 phase-14 + 4 phase-15) pass green.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.2-fix-journey-phase-14-schema-foundation-and-shared-types-completes-successfully/15.2-CONTEXT.md
@.planning/phases/15.2-fix-journey-phase-14-schema-foundation-and-shared-types-completes-successfully/15.2-RESEARCH.md
@.planning/phases/15.1-fix-journey-build-and-start-completes-successfully-blocker/15.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore infrastructure files from HEAD and add NODE_ENV to playwright config</name>
  <files>
    .auto-gsd/journey-tests/playwright.config.ts
    .auto-gsd/journey-tests/journey-build-and-start.spec.ts
    .auto-gsd/journey-tests/journey-phase-15.spec.ts
  </files>
  <action>
    Restore 3 files from git HEAD using:
    ```
    git -C .. checkout HEAD -- my-project/.auto-gsd/journey-tests/playwright.config.ts my-project/.auto-gsd/journey-tests/journey-build-and-start.spec.ts my-project/.auto-gsd/journey-tests/journey-phase-15.spec.ts
    ```
    (Path prefix `my-project/` is required because the git repo root is one level above the working directory.)

    Then modify `playwright.config.ts` to add `env: { NODE_ENV: 'development' }` inside the `webServer` block, per CONTEXT.md locked decision. The final webServer block should be:
    ```typescript
    webServer: {
      command: 'pnpm dev',
      url: 'http://localhost:5173',
      timeout: 120 * 1000,
      reuseExistingServer: true,
      cwd: projectRoot,
      env: {
        NODE_ENV: 'development',
      },
    },
    ```

    Do NOT modify journey-phase-15.spec.ts content — HEAD already has the lowercase snake_case fix from Phase 15.1.
    Do NOT modify journey-build-and-start.spec.ts content — HEAD version is correct (pnpm build, 180s timeout, dist verification).
    Do NOT touch package.json or node_modules — @playwright/test is already installed from Phase 14.3.
  </action>
  <verify>
    Verify restored files have correct content:
    - `playwright.config.ts` contains `webServer` block with `pnpm dev`, 120s timeout, `reuseExistingServer: true`, and `env: { NODE_ENV: 'development' }`
    - `journey-build-and-start.spec.ts` contains `pnpm build`, `180000` timeout, `projectRoot`, `dist` verification test
    - `journey-phase-15.spec.ts` contains `SERVER_URL`, `page.request`, `feature_building` (lowercase snake_case)
  </verify>
  <done>
    Three infrastructure files restored with correct content. playwright.config.ts has NODE_ENV=development in webServer env.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write journey-phase-14.spec.ts with real diagnostic endpoint assertions per CONTEXT.md</name>
  <files>
    .auto-gsd/journey-tests/journey-phase-14.spec.ts
  </files>
  <action>
    Write `.auto-gsd/journey-tests/journey-phase-14.spec.ts` fresh (NOT from HEAD — CONTEXT.md decisions differ from HEAD).

    The file must contain 4 tests inside `test.describe('Phase 14: Schema Foundation and Shared Types', () => {...})`:

    **Test 1: graph_snapshots table CRUD**
    - `test.setTimeout(60000)`
    - `const SERVER_URL = process.env.SERVER_URL || 'http://localhost:3100'` (defined at module top)
    - `page.request.get(`${SERVER_URL}/api/debug/graph-snapshots-table`)`
    - Assert: `response.status()` is 200 with message `'GET /api/debug/graph-snapshots-table should return 200'`
    - Assert: `body.ok` is `true` with message `'graph_snapshots CRUD diagnostic should return ok: true'`
    - Assert: `body.positionsStoredInGraphJson` is `true` with message `'positions should be stored inside graphJson'`
    - Assert: `body.crud.insert`, `body.crud.read`, `body.crud.delete` are all `true` with descriptive messages

    **Test 2: intent_sessions table CRUD**
    - `test.setTimeout(60000)`
    - `page.request.get(`${SERVER_URL}/api/debug/intent-sessions-table`)`
    - Assert: `response.status()` is 200 with message
    - Assert: `body.ok` is `true` with message `'intent_sessions CRUD diagnostic should return ok: true'`
    - Assert: `body.crud.insert`, `body.crud.read`, `body.crud.delete` are all `true` with descriptive messages

    **Test 3: shared types compile and IntentCategory runtime access**
    - `test.setTimeout(60000)`
    - `page.request.get(`${SERVER_URL}/api/debug/shared-types`)`
    - Assert: `response.status()` is 200
    - Assert: `body.ok` is `true` with message `'shared-types diagnostic should return ok: true (server running proves TypeScript compilation)'`
    - Assert: `body.hasIntentCategory` is `true` with message `'IntentCategory const (runtime companion to IntentSession/SnapshotMeta types) should be accessible'`

    **Test 4: delta-threshold snapshot behavior (LAST test — changes watch root)**
    - `test.setTimeout(60000)`
    - Dynamic import `fs`, `path`, `os` at runtime (ESM compatibility)
    - Create temp directory with `mkdtempSync(join(tmpdir(), 'archlens-diag-'))`
    - `const FILE_COUNT = 18` (within the 15-20 range per CONTEXT.md)
    - Write FILE_COUNT valid JS files BEFORE switching watch root:
      ```
      writeFileSync(join(tmpDir, `module${i}.js`), `export const value${i} = ${i};\nexport function fn${i}() { return ${i}; }\n`)
      ```
    - `page.request.post(`${SERVER_URL}/api/watch`, { data: { directory: tmpDir } })`
    - Assert POST returns 200
    - `await page.waitForTimeout(5000)` — fixed 5s wait per CONTEXT.md
    - `page.request.get(`${SERVER_URL}/api/timeline`)`
    - Assert GET returns 200
    - Parse response as `unknown[]`
    - Assert `snapshots.length > 0` with message: `'Expected at least 1 snapshot after ${FILE_COUNT} file events but got 0 — pipeline may not have scanned yet'`
    - Assert `snapshots.length < FILE_COUNT` with message: `'Expected snapshot count < event count but got ${snapshots.length} snapshots for ${FILE_COUNT} events — delta throttling may not be active'`

    **IMPORTANT per CONTEXT.md locked decisions:**
    - All assertions must have descriptive custom failure messages
    - Use `page.request` (Playwright's request context), NOT `page.goto` + DOM scraping
    - Target port 3100 (SERVER_URL), NOT port 5173 (BASE_URL)
    - Files written BEFORE `/api/watch` switch so initial scan captures them as a burst
    - 5s fixed wait (NOT polling loop) after POST /api/watch
  </action>
  <verify>
    Run the journey tests to verify all 12 pass:
    ```bash
    cd .auto-gsd/journey-tests && node_modules/.bin/playwright.cmd test --reporter=list 2>&1
    ```
    (On Windows, use `.cmd` variant. If `playwright.cmd` is not available, try `npx playwright test`.)

    All 12 tests should pass:
    - 4 from journey-build-and-start.spec.ts
    - 4 from journey-phase-14.spec.ts
    - 4 from journey-phase-15.spec.ts
  </verify>
  <done>
    journey-phase-14.spec.ts contains 4 real tests with diagnostic endpoint assertions using page.request against SERVER_URL:3100. Delta-threshold test uses 18 files, fixed 5s wait. All 12 journey tests pass green.
  </done>
</task>

</tasks>

<verification>
1. `playwright.config.ts` has webServer block with `env: { NODE_ENV: 'development' }` and 120s timeout
2. `journey-build-and-start.spec.ts` has `pnpm build` with 180s timeout and dist verification
3. `journey-phase-14.spec.ts` has 4 real tests using `page.request.get/post` against `SERVER_URL:3100`
4. `journey-phase-15.spec.ts` has `page.request` tests with lowercase snake_case `validCategories`
5. All 12 journey tests pass green end-to-end
</verification>

<success_criteria>
- All 12 journey tests pass (4 build-and-start + 4 phase-14 + 4 phase-15)
- Phase 14 tests use real diagnostic endpoint assertions (not body.length placeholders)
- Delta-threshold test uses 18 files (in 15-20 range) and fixed 5s wait
- playwright.config.ts explicitly sets NODE_ENV=development
- No regressions to Phase 15 or build-and-start test suites
</success_criteria>

<output>
After completion, create `.planning/phases/15.2-fix-journey-phase-14-schema-foundation-and-shared-types-completes-successfully/15.2-01-SUMMARY.md`
</output>
