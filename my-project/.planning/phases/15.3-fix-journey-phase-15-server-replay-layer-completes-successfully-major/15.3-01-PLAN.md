---
phase: 15.3-fix-journey-phase-15-server-replay-layer-completes-successfully-major
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .auto-gsd/journey-tests/playwright.config.ts
  - .auto-gsd/journey-tests/journey-build-and-start.spec.ts
  - .auto-gsd/journey-tests/journey-phase-14.spec.ts
  - .auto-gsd/journey-tests/journey-phase-15.spec.ts
autonomous: true
requirements: [INFRA-03]

must_haves:
  truths:
    - "GET /api/timeline returns snapshot metadata with id, sequenceNumber, timestamp, summary, triggerFiles after writing files to a temp dir and switching watch root"
    - "GET /api/snapshot/:id returns a complete graph with nodes matching written file paths, edges from real imports, and a positions object"
    - "GET /api/snapshot/999999999 returns 404 and GET /api/snapshot/not-a-number returns 400"
    - "GET /api/intents returns at least one IntentSession with valid category, confidence 0-1, subtasks array, objective string, and sessionId string"
    - "GET /api/timeline responds within 5 seconds while files are being written concurrently, and new snapshots appear after the write burst"
    - "All 12 journey tests pass green (4 build-and-start + 4 phase-14 + 4 phase-15)"
  artifacts:
    - path: ".auto-gsd/journey-tests/playwright.config.ts"
      provides: "webServer block with pnpm dev, 120s timeout, NODE_ENV=development, ESM projectRoot"
    - path: ".auto-gsd/journey-tests/journey-build-and-start.spec.ts"
      provides: "4 build/start tests with pnpm build 180s timeout and dist verification"
    - path: ".auto-gsd/journey-tests/journey-phase-14.spec.ts"
      provides: "4 Phase 14 tests with real diagnostic endpoint assertions against SERVER_URL:3100"
    - path: ".auto-gsd/journey-tests/journey-phase-15.spec.ts"
      provides: "4 Phase 15 tests with deep API assertions per CONTEXT.md decisions"
  key_links:
    - from: ".auto-gsd/journey-tests/journey-phase-15.spec.ts"
      to: "packages/server/src/plugins/timeline.ts"
      via: "HTTP requests to /api/timeline, /api/snapshot/:id, /api/intents"
      pattern: "page\\.request\\.(get|post).*SERVER_URL.*/api/(timeline|snapshot|intents)"
    - from: ".auto-gsd/journey-tests/journey-phase-15.spec.ts"
      to: "packages/server/src/index.ts"
      via: "POST /api/watch to switch watch root and trigger pipeline"
      pattern: "page\\.request\\.post.*SERVER_URL.*/api/watch"
    - from: ".auto-gsd/journey-tests/playwright.config.ts"
      to: "packages/server/src/index.ts"
      via: "webServer block starts pnpm dev which launches Fastify on port 3100"
      pattern: "webServer.*pnpm dev"
---

<objective>
Restore 3 regressed journey test infrastructure files from git HEAD and write journey-phase-15.spec.ts with deep CONTEXT.md-level assertions verifying all 4 Phase 15 Server Replay Layer success criteria via real HTTP requests.

Purpose: Auto-gsd overwrote all 4 journey test files with body-length stubs after Phase 15.2. The server code is fully built and working. This phase restores the infrastructure and writes the Phase 15 tests with deeper assertions than the HEAD version had — including sequential number monotonicity, node ID matching against written files, edge presence from real imports, full IntentSession shape validation, timestamp epoch ranges, HTTP error cases (404/400), and concurrent write+read verification.

Output: 4 passing journey test files (12 total tests) with real HTTP API assertions against the running Fastify server.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.3-fix-journey-phase-15-server-replay-layer-completes-successfully-major/15.3-CONTEXT.md
@.planning/phases/15.3-fix-journey-phase-15-server-replay-layer-completes-successfully-major/15.3-RESEARCH.md
@.planning/phases/15.2-fix-journey-phase-14-schema-foundation-and-shared-types-completes-successfully/15.2-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore 3 regressed infrastructure files from git HEAD</name>
  <files>
    .auto-gsd/journey-tests/playwright.config.ts
    .auto-gsd/journey-tests/journey-build-and-start.spec.ts
    .auto-gsd/journey-tests/journey-phase-14.spec.ts
  </files>
  <action>
Restore the 3 infrastructure files that auto-gsd overwrote with stubs. The git repo root is one level above the working directory (`F:\Directory\FrontierIntelligenceHack`), so the restore command must use the `my-project/` prefix:

```bash
git -C .. checkout HEAD -- my-project/.auto-gsd/journey-tests/playwright.config.ts my-project/.auto-gsd/journey-tests/journey-build-and-start.spec.ts my-project/.auto-gsd/journey-tests/journey-phase-14.spec.ts
```

After restore, verify the 3 files contain the correct content:

1. **playwright.config.ts** must contain:
   - `import path from 'path'` and `import { fileURLToPath } from 'url'` (ESM)
   - `const __dirname = path.dirname(fileURLToPath(import.meta.url))`
   - `const projectRoot = path.resolve(__dirname, '../../')`
   - `webServer` block with `command: 'pnpm dev'`, `timeout: 120 * 1000`, `reuseExistingServer: true`, `cwd: projectRoot`
   - `env: { NODE_ENV: 'development' }` inside the webServer block

2. **journey-build-and-start.spec.ts** must contain:
   - `pnpm build` (not `npm run build`)
   - `timeout: 180000` (not 30000)
   - `cwd: projectRoot`
   - 4 tests including `Build outputs exist in dist folders`

3. **journey-phase-14.spec.ts** must contain:
   - `const SERVER_URL = process.env.SERVER_URL || 'http://localhost:3100'`
   - `page.request.get(\`\${SERVER_URL}/api/debug/...\`)` calls
   - 4 real diagnostic endpoint tests (not body-length placeholders)
   - Delta-threshold test with `FILE_COUNT = 18` and 5s fixed wait

If any file does not contain the expected content after restore, check that HEAD has the correct version. If HEAD is also a stub, reconstruct manually per 15.2-01-SUMMARY.md patterns.
  </action>
  <verify>
Inspect each restored file:
- `grep "webServer" .auto-gsd/journey-tests/playwright.config.ts` returns a match
- `grep "NODE_ENV" .auto-gsd/journey-tests/playwright.config.ts` returns a match
- `grep "pnpm build" .auto-gsd/journey-tests/journey-build-and-start.spec.ts` returns a match
- `grep "180000" .auto-gsd/journey-tests/journey-build-and-start.spec.ts` returns a match
- `grep "SERVER_URL" .auto-gsd/journey-tests/journey-phase-14.spec.ts` returns a match
- `grep "api/debug" .auto-gsd/journey-tests/journey-phase-14.spec.ts` returns a match
  </verify>
  <done>All 3 infrastructure files restored to their HEAD versions with webServer block, NODE_ENV=development, pnpm build 180s, and real diagnostic endpoint assertions</done>
</task>

<task type="auto">
  <name>Task 2: Write journey-phase-15.spec.ts with deep CONTEXT.md-level assertions</name>
  <files>
    .auto-gsd/journey-tests/journey-phase-15.spec.ts
  </files>
  <action>
Write `.auto-gsd/journey-tests/journey-phase-15.spec.ts` fresh with 4 tests matching the Phase 15 success criteria test names. Use `page.request` against `SERVER_URL` (port 3100), temp directory isolation per test, TypeScript files with real import statements, and polling with 30s deadlines.

**CRITICAL DEVIATIONS FROM CONTEXT.md (per RESEARCH.md findings):**
- Positions assertion: Use `typeof positions === 'object' && positions !== null` — NOT `Object.keys(positions).length === nodes.length` (layout_positions table is empty until Phase 6, so the strict assertion will always fail)
- startSnapshotId: Assert `session.startSnapshotId === null || typeof session.startSnapshotId === 'number'` — handles both valid states since no snapshot may exist when session opens

**File structure:**
```typescript
import { test, expect } from '@playwright/test';
import fs from 'fs';
import path from 'path';
import os from 'os';

const SERVER_URL = process.env.SERVER_URL || 'http://localhost:3100';

test.describe('Phase 15: Server Replay Layer', () => {
  // 4 tests below
});
```

**Test 1: GET /api/timeline** (exact test name: `` `GET /api/timeline` returns a list of snapshot metadata with sequence numbers and timestamps after file changes occur ``)
- Create temp dir, write 3+ TypeScript files with real import statements (e.g., `b.ts` exports, `a.ts` imports from `./b.js`, `c.ts` imports from `./a.js`)
- POST /api/watch to switch watch root to temp dir
- Poll GET /api/timeline with 30s deadline, 1s interval until snapshots.length > 0
- Shape validation: id (number), sequenceNumber (number), timestamp (number), summary (string), triggerFiles (array)
- Timestamp epoch range: > 1735689600000 (2025-01-01) and < 1893456000000 (~2030)
- Monotonic sequenceNumbers: if snapshots.length > 1, each sequenceNumber > previous
- Cleanup temp dir in finally block

**Test 2: GET /api/snapshot/:id** (exact test name: `` `GET /api/snapshot/:id` returns a complete graph snapshot (nodes, edges, positions) reconstructed from the nearest checkpoint in at most 50 replay steps ``)
- Create temp dir, write `b.ts` (export) and `a.ts` (imports from `./b.js`) to guarantee edges
- POST /api/watch, poll for snapshot ID via GET /api/timeline
- GET /api/snapshot/:id and validate: id (number), sequenceNumber (number), nodes (array), edges (array), positions (object, not null)
- Node ID matching: `snap.nodes` array entries should have `.id` properties containing written file absolute paths (use `expect(nodeIds).toContain(fileA)` and `expect(nodeIds).toContain(fileB)`)
- Edge count: `snap.edges.length > 0` (at least 1 edge from the import)
- Error cases: GET /api/snapshot/999999999 returns 404, GET /api/snapshot/not-a-number returns 400
- Cleanup in finally

**Test 3: IntentAnalyzer** (exact test name: `The \`IntentAnalyzer\` classifies a realistic sequence of architectural events into one of the 4-6 coarse categories and returns a confidence score`)
- Create temp dir, write 4+ `.spec.ts` files with imports from a `test-framework.ts` file to strongly signal TEST_WRITING category
- POST /api/watch, poll GET /api/timeline until snapshots appear (proves IntentAnalyzer has processed deltas)
- GET /api/intents and assert sessions.length > 0 UNCONDITIONALLY (no vacuous if-guard)
- Full shape validation on first session:
  - category: must be one of `['feature_building', 'bug_fixing', 'refactoring', 'test_writing', 'dependency_update', 'cleanup']`
  - confidence: typeof number, >= 0, <= 1
  - subtasks: Array.isArray
  - objective: typeof string
  - sessionId: typeof string
  - startSnapshotId: null or typeof number
- Cleanup in finally

**Test 4: Concurrency** (exact test name: `Writing files during an active session does not cause the pipeline to pause — new events continue to arrive while the replay read path is active`)
- Create temp dir, write 6 initial .ts files, POST /api/watch, wait for initial snapshot via polling
- Write 12+ more .ts files in rapid succession (to exceed MINOR_THRESHOLD=10)
- Immediately call GET /api/timeline and measure response time — must be < 5000ms
- After burst, poll GET /api/timeline with 30s deadline until `finalSnapshots.length > 0` to confirm pipeline still alive
- Cleanup in finally

All tests must use `test.setTimeout(60000)` and clean up temp dirs in `finally` blocks.
  </action>
  <verify>
Run all 12 journey tests:
```bash
cd .auto-gsd/journey-tests && npx playwright test --reporter=list 2>&1
```
Expected: 12 tests passed (4 build-and-start + 4 phase-14 + 4 phase-15), 0 failed.

If the dev server is not running, start it first: `pnpm dev` from project root, then run tests.

Additionally verify the specific Phase 15 tests:
```bash
cd .auto-gsd/journey-tests && npx playwright test journey-phase-15.spec.ts --reporter=list 2>&1
```
Expected: 4 tests passed, 0 failed.
  </verify>
  <done>
journey-phase-15.spec.ts contains 4 real tests with deep assertions per CONTEXT.md:
1. Timeline test validates shape (id, sequenceNumber, timestamp, summary, triggerFiles), epoch range, and monotonic sequence numbers
2. Snapshot test validates nodes matching written file paths, edges from real imports, positions as object, and error cases (404/400)
3. Intent test unconditionally asserts session exists with valid category, confidence 0-1, subtasks array, objective, sessionId, and startSnapshotId
4. Concurrency test proves pipeline responds within 5s during write burst and new snapshots appear after
All 12 journey tests pass green.
  </done>
</task>

</tasks>

<verification>
1. All 12 journey tests pass: `cd .auto-gsd/journey-tests && npx playwright test --reporter=list`
2. playwright.config.ts contains webServer block with NODE_ENV=development
3. journey-build-and-start.spec.ts uses pnpm build with 180s timeout
4. journey-phase-14.spec.ts has real diagnostic endpoint assertions against SERVER_URL:3100
5. journey-phase-15.spec.ts has 4 tests with real HTTP assertions (not body-length placeholders)
6. No test uses BASE_URL (5173) for API calls — all API tests use SERVER_URL (3100)
7. All Phase 15 tests create temp dirs and clean up in finally blocks
</verification>

<success_criteria>
12 journey tests pass green end-to-end. The 4 Phase 15 tests exercise the real /api/timeline, /api/snapshot/:id, and /api/intents endpoints with deep shape, value, and error case validation per CONTEXT.md locked decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/15.3-fix-journey-phase-15-server-replay-layer-completes-successfully-major/15.3-01-SUMMARY.md`
</output>
