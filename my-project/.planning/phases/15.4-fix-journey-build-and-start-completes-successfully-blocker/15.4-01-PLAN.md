---
phase: 15.4-fix-journey-build-and-start-completes-successfully-blocker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .auto-gsd/journey-tests/playwright.config.ts
  - .auto-gsd/journey-tests/journey-build-and-start.spec.ts
  - .auto-gsd/journey-tests/journey-phase-14.spec.ts
  - .auto-gsd/journey-tests/journey-phase-15.spec.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "All 4 journey test files match their git HEAD versions exactly (no staged or unstaged modifications)"
    - "pnpm exec playwright test from .auto-gsd/journey-tests/ reports 12 expected, 0 skipped, 0 unexpected"
    - "playwright.config.ts contains webServer block with workers:1 and NODE_ENV:development"
    - "journey-build-and-start.spec.ts uses pnpm build with 180s timeout"
    - "journey-phase-14.spec.ts uses SERVER_URL port 3100 for diagnostic endpoint assertions"
    - "journey-phase-15.spec.ts has 252 lines with timeline, snapshot, intent, and concurrency assertions"
  artifacts:
    - path: ".auto-gsd/journey-tests/playwright.config.ts"
      provides: "Playwright config with webServer block, workers:1, NODE_ENV:development"
      contains: "workers: 1"
    - path: ".auto-gsd/journey-tests/journey-build-and-start.spec.ts"
      provides: "Build and start journey tests (4 tests)"
      contains: "pnpm build"
    - path: ".auto-gsd/journey-tests/journey-phase-14.spec.ts"
      provides: "Phase 14 journey tests with diagnostic endpoint assertions (4 tests)"
      contains: "SERVER_URL"
    - path: ".auto-gsd/journey-tests/journey-phase-15.spec.ts"
      provides: "Phase 15 journey tests with timeline/snapshot/intent assertions (4 tests)"
      contains: "api/timeline"
  key_links:
    - from: ".auto-gsd/journey-tests/playwright.config.ts"
      to: "pnpm dev (webServer)"
      via: "webServer.command auto-start"
      pattern: "command.*pnpm dev"
    - from: ".auto-gsd/journey-tests/journey-phase-14.spec.ts"
      to: "http://localhost:3100/api/debug/*"
      via: "page.request.get against SERVER_URL"
      pattern: "SERVER_URL.*3100"
    - from: ".auto-gsd/journey-tests/journey-phase-15.spec.ts"
      to: "http://localhost:3100/api/timeline"
      via: "page.request.get against SERVER_URL"
      pattern: "SERVER_URL.*3100"
---

<objective>
Restore 4 auto-GSD-overwritten journey test files from git HEAD and verify all 12 tests pass green.

Purpose: This is the 4th occurrence of the same regression pattern where the auto-GSD orchestrator overwrites journey test files with placeholder stubs. The committed HEAD versions (proven green at 2026-03-17T05:16:41Z with 12/12 pass) contain the real deep assertions. Restoring them unblocks journey test validation for Phase 15 completion.

Output: 4 restored journey test files matching git HEAD exactly, with 12/12 tests passing green.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/15.4-fix-journey-build-and-start-completes-successfully-blocker/15.4-CONTEXT.md
@.planning/phases/15.4-fix-journey-build-and-start-completes-successfully-blocker/15.4-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore 4 journey test files from git HEAD</name>
  <files>
    .auto-gsd/journey-tests/playwright.config.ts
    .auto-gsd/journey-tests/journey-build-and-start.spec.ts
    .auto-gsd/journey-tests/journey-phase-14.spec.ts
    .auto-gsd/journey-tests/journey-phase-15.spec.ts
  </files>
  <action>
    Restore all 4 overwritten journey test files from git HEAD. The git repo root is one level above the working directory, so paths must be prefixed with `my-project/`.

    **IMPORTANT:** Use `git checkout HEAD --` (not `git restore`) for each file. This handles both staged and unstaged changes simultaneously.

    Restore in this order (playwright config first so test runner config is correct before any validation):

    1. `git -C .. checkout HEAD -- "my-project/.auto-gsd/journey-tests/playwright.config.ts"`
       - Verify: file contains `workers: 1` and `webServer` block with `NODE_ENV: 'development'`

    2. `git -C .. checkout HEAD -- "my-project/.auto-gsd/journey-tests/journey-build-and-start.spec.ts"`
       - Verify: file contains `pnpm build` and `timeout: 180000`

    3. `git -C .. checkout HEAD -- "my-project/.auto-gsd/journey-tests/journey-phase-14.spec.ts"`
       - Verify: file contains `SERVER_URL` and `/api/debug/graph-snapshots-table`

    4. `git -C .. checkout HEAD -- "my-project/.auto-gsd/journey-tests/journey-phase-15.spec.ts"`
       - Verify: file has ~252 lines and contains `/api/timeline` and `/api/snapshot/`

    After all 4 restores, run `git -C .. status -- "my-project/.auto-gsd/journey-tests/"` to confirm no modified files remain.

    **If any test file fails to restore** (pathspec error): verify the path prefix is `my-project/` and retry.
    **If git status still shows modifications after restore**: the file may have been staged separately. Run `git -C .. checkout HEAD -- <path>` again which resets both index and worktree.
  </action>
  <verify>
    Run: `git -C .. diff HEAD -- "my-project/.auto-gsd/journey-tests/playwright.config.ts" "my-project/.auto-gsd/journey-tests/journey-build-and-start.spec.ts" "my-project/.auto-gsd/journey-tests/journey-phase-14.spec.ts" "my-project/.auto-gsd/journey-tests/journey-phase-15.spec.ts"`
    Expected: No output (all files match HEAD exactly).

    Also verify line counts:
    - playwright.config.ts: ~28 lines (not 12)
    - journey-build-and-start.spec.ts: ~43 lines (not 25)
    - journey-phase-14.spec.ts: ~85 lines (not 29)
    - journey-phase-15.spec.ts: ~252 lines (not 29)
  </verify>
  <done>All 4 journey test files match git HEAD exactly. git diff HEAD shows no differences for these files. git status shows no modifications in .auto-gsd/journey-tests/ for these 4 files.</done>
</task>

<task type="auto">
  <name>Task 2: Run full Playwright test suite and confirm 12/12 pass</name>
  <files>
    .auto-gsd/journey-tests/results.json
  </files>
  <action>
    Run the full Playwright test suite from the standalone journey-tests directory to confirm all 12 tests pass green.

    1. Navigate to the journey-tests directory and run the suite:
       ```bash
       cd .auto-gsd/journey-tests && pnpm exec playwright test
       ```
       The `webServer` block in playwright.config.ts will auto-start `pnpm dev` from the project root (reuseExistingServer: true). Tests run serially (workers: 1).

    2. Expected result: **12 expected, 0 skipped, 0 unexpected**
       - 4 tests from journey-build-and-start.spec.ts (build, dist folders, page loads, no console errors)
       - 4 tests from journey-phase-14.spec.ts (snapshots CRUD, sessions CRUD, shared types, delta threshold)
       - 4 tests from journey-phase-15.spec.ts (timeline shape, snapshot shape, intent sessions, concurrency)

    3. The test run automatically generates a fresh `results.json` in the journey-tests directory (via the `json` reporter in playwright.config.ts).

    **If any test fails after restore due to server changes:**
    Per CONTEXT.md locked decision: fix forward by updating the test to match current server behavior while preserving assertion depth. Do NOT weaken assertions to body-length checks.

    **If webServer fails to start:**
    Check that `pnpm dev` works from the project root. The webServer config uses `cwd: projectRoot` (resolved to `../../` from the config file). Timeout is 120s for cold start.

    **If build test times out:**
    Ensure journey-build-and-start.spec.ts has `timeout: 180000` (180s) for the pnpm build step. The placeholder version had 30s which is insufficient for TypeScript monorepo compilation.
  </action>
  <verify>
    Run: `cd .auto-gsd/journey-tests && pnpm exec playwright test`
    Expected: Exit code 0, output contains "12 passed"

    Also verify results.json exists and contains:
    - "expected": 12 (or check numPassed === 12)
    - "unexpected": 0 (or check numFailed === 0)
  </verify>
  <done>All 12 journey tests pass green (12 expected, 0 unexpected). Fresh results.json generated confirming the passing run. No test failures or timeouts.</done>
</task>

</tasks>

<verification>
1. `git -C .. diff HEAD -- "my-project/.auto-gsd/journey-tests/"` shows no differences for the 4 test files
2. `pnpm exec playwright test` from `.auto-gsd/journey-tests/` reports 12 passed, 0 failed
3. `results.json` in `.auto-gsd/journey-tests/` reflects 12 expected, 0 unexpected
4. playwright.config.ts contains `workers: 1`, `webServer` block, and `NODE_ENV: 'development'`
5. journey-phase-14.spec.ts uses `SERVER_URL` (port 3100), not `BASE_URL` (port 5173)
6. journey-phase-15.spec.ts has ~252 lines with deep API assertions
</verification>

<success_criteria>
- All 4 journey test files restored to exact git HEAD content
- 12/12 journey tests pass green with no failures or skips
- Fresh results.json generated confirming the passing run
- No placeholder content remaining (no body-length-only assertions)
</success_criteria>

<output>
After completion, create `.planning/phases/15.4-fix-journey-build-and-start-completes-successfully-blocker/15.4-01-SUMMARY.md`
</output>
