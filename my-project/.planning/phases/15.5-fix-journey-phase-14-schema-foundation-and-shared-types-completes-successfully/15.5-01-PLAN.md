---
phase: 15.5-fix-journey-phase-14-schema-foundation-and-shared-types-completes-successfully
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .auto-gsd/journey-tests/journey-build-and-start.spec.ts
  - .auto-gsd/journey-tests/journey-phase-14.spec.ts
  - .auto-gsd/journey-tests/journey-phase-15.spec.ts
  - .auto-gsd/journey-tests/playwright.config.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "All 4 journey test files match their git HEAD committed versions (not auto-gsd stubs)"
    - "All 12 journey tests pass green (4 build-and-start + 4 phase-14 + 4 phase-15)"
    - "Diagnostic endpoints respond with expected JSON shape before tests run"
    - "Root cause of auto-gsd overwrite pattern is documented for future reference"
  artifacts:
    - path: ".auto-gsd/journey-tests/journey-build-and-start.spec.ts"
      provides: "Build and start journey tests with real pnpm build assertions"
      contains: "pnpm build"
    - path: ".auto-gsd/journey-tests/journey-phase-14.spec.ts"
      provides: "Phase 14 journey tests with real diagnostic endpoint assertions"
      contains: "SERVER_URL"
    - path: ".auto-gsd/journey-tests/journey-phase-15.spec.ts"
      provides: "Phase 15 journey tests with real timeline/snapshot/intent assertions"
      contains: "SERVER_URL"
    - path: ".auto-gsd/journey-tests/playwright.config.ts"
      provides: "Real Playwright config with workers:1, webServer, and NODE_ENV"
      contains: "workers"
  key_links:
    - from: ".auto-gsd/journey-tests/playwright.config.ts"
      to: "pnpm dev"
      via: "webServer config block"
      pattern: "webServer.*pnpm dev"
    - from: ".auto-gsd/journey-tests/journey-phase-14.spec.ts"
      to: "http://localhost:3100/api/debug/*"
      via: "page.request.get with SERVER_URL"
      pattern: "SERVER_URL.*3100"
    - from: ".auto-gsd/journey-tests/journey-phase-15.spec.ts"
      to: "http://localhost:3100/api/timeline"
      via: "page.request.get with SERVER_URL"
      pattern: "SERVER_URL.*3100"
---

<objective>
Restore 4 auto-gsd-overwritten journey test files from git HEAD, verify all 12 journey tests pass green, and document the root cause of the recurring overwrite pattern.

Purpose: This is the 4th occurrence (15.1, 15.2, 15.4, now 15.5) of auto-gsd overwriting real journey test files with placeholder stubs. Breaking the cycle requires both restoration and root cause documentation. The auto-gsd orchestrator's journey-test-generator.ts unconditionally regenerates all journey test files after every phase completion, replacing deep endpoint assertions with body-length placeholder checks.

Output: 4 restored test files, 12/12 passing tests, root cause documented in SUMMARY.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15.5-fix-journey-phase-14-schema-foundation-and-shared-types-completes-successfully/15.5-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore journey test files from git HEAD and verify 12/12 tests pass</name>
  <files>
    .auto-gsd/journey-tests/journey-build-and-start.spec.ts
    .auto-gsd/journey-tests/journey-phase-14.spec.ts
    .auto-gsd/journey-tests/journey-phase-15.spec.ts
    .auto-gsd/journey-tests/playwright.config.ts
  </files>
  <action>
    **Step 1: Confirm files need restoration.**
    Run `git diff HEAD -- .auto-gsd/journey-tests/journey-phase-14.spec.ts` from within the my-project directory. If diff output exists, files have been overwritten by auto-gsd stubs and need restoration. If no diff, files are already correct (skip to verification).

    **Step 2: Restore all 4 files from git HEAD.**
    Run from the my-project directory (git resolves paths relative to repo root automatically):
    ```bash
    git checkout HEAD -- .auto-gsd/journey-tests/journey-build-and-start.spec.ts
    git checkout HEAD -- .auto-gsd/journey-tests/journey-phase-14.spec.ts
    git checkout HEAD -- .auto-gsd/journey-tests/journey-phase-15.spec.ts
    git checkout HEAD -- .auto-gsd/journey-tests/playwright.config.ts
    ```
    This handles both staged and unstaged changes simultaneously (confirmed in Phase 15.4 decision).

    **Step 3: Verify restoration correctness.**
    Confirm the restored files have real content (not stubs):
    - `journey-phase-14.spec.ts` must contain `SERVER_URL` (not `BASE_URL`)
    - `playwright.config.ts` must contain `workers: 1` and `webServer` and `NODE_ENV`
    - `journey-phase-15.spec.ts` must contain `SERVER_URL` and `/api/timeline`

    **Step 4: Ensure @playwright/test dependencies are installed.**
    Run `npm install` in `.auto-gsd/journey-tests/` (standalone package.json, not pnpm workspace).

    **Step 5: Pre-flight diagnostic endpoint check.**
    The Playwright webServer config starts `pnpm dev` automatically if not running (`reuseExistingServer: true`). However, pre-check that port 3100 will be reachable by verifying the server can start. If port 3100 is already live, verify with:
    ```bash
    curl -sf http://localhost:3100/api/debug/graph-snapshots-table
    curl -sf http://localhost:3100/api/debug/intent-sessions-table
    curl -sf http://localhost:3100/api/debug/shared-types
    ```
    If server is not running, the Playwright webServer block will start it. The key is that `NODE_ENV=development` is set in the real playwright.config.ts (required for diagnostic endpoints).

    **Step 6: Run all 12 journey tests.**
    ```bash
    cd .auto-gsd/journey-tests && npx playwright test
    ```
    Expected output: 12 passed, 0 failed.

    **Step 7: If any tests fail, diagnose and fix within this task** (per CONTEXT.md locked decision: "If any tests fail after restoration, diagnose and fix within this phase"). Common failure modes:
    - Port 3100 not reachable: ensure `pnpm dev` starts both Vite (5173) and Fastify (3100)
    - Missing @playwright/test: re-run `npm install` in journey-tests directory
    - Missing Chromium: run `npx playwright install chromium` in journey-tests directory
    - Stale build artifacts: run `pnpm build` from project root before tests
  </action>
  <verify>
    1. `git diff HEAD -- .auto-gsd/journey-tests/` shows no differences (files match HEAD)
    2. `grep "SERVER_URL" .auto-gsd/journey-tests/journey-phase-14.spec.ts` returns matches
    3. `grep "workers" .auto-gsd/journey-tests/playwright.config.ts` returns matches
    4. `cd .auto-gsd/journey-tests && npx playwright test` outputs "12 passed"
  </verify>
  <done>All 4 journey test files restored to their git HEAD versions. All 12 journey tests (4 build-and-start + 4 phase-14 + 4 phase-15) pass green with 0 failures.</done>
</task>

<task type="auto">
  <name>Task 2: Document root cause and commit restored files</name>
  <files>
    .auto-gsd/journey-tests/journey-build-and-start.spec.ts
    .auto-gsd/journey-tests/journey-phase-14.spec.ts
    .auto-gsd/journey-tests/journey-phase-15.spec.ts
    .auto-gsd/journey-tests/playwright.config.ts
  </files>
  <action>
    **Step 1: Document root cause in the SUMMARY.**
    The root cause is definitively identified in 15.5-RESEARCH.md. Include in the SUMMARY:

    - **Source:** `OttoGSD/src/orchestrator/journey-test-generator.ts`
    - **Trigger:** `phase:complete` event in `journey-orchestrator.ts` fires `generateTestScripts()` after EVERY phase
    - **Mechanism:** Deletes ALL `journey-*.spec.ts` files, regenerates from JOURNEYS.md as body-length stubs via `generatePlaywrightSpec()`, also regenerates `playwright.config.ts` with minimal config (no workers:1, no webServer, no NODE_ENV)
    - **Prevention:** Requires OttoGSD binary changes (no "skip if file exists" guard); per CONTEXT.md and Phase 15.4 decision, accept the restore-from-HEAD playbook pattern
    - **Recommendation for future:** If OttoGSD is updated, add a `skipExistingTests: true` option or check for file content beyond stubs before overwriting

    **Step 2: Commit the restored files.**
    Use gsd-tools commit:
    ```bash
    node ./.claude/get-shit-done/bin/gsd-tools.cjs commit "fix(15.5-01): restore journey test files from git HEAD, verify 12/12 pass" \
      --files .auto-gsd/journey-tests/journey-build-and-start.spec.ts \
               .auto-gsd/journey-tests/journey-phase-14.spec.ts \
               .auto-gsd/journey-tests/journey-phase-15.spec.ts \
               .auto-gsd/journey-tests/playwright.config.ts
    ```

    Note: If the files are already at HEAD (no diff), the commit may be empty. In that case, the SUMMARY should note that files were already correct and only verification was needed.
  </action>
  <verify>
    1. git log shows new commit with restored files (or note that files were already at HEAD)
    2. SUMMARY.md exists in phase directory with root cause documentation
  </verify>
  <done>Root cause of auto-gsd overwrite pattern documented in SUMMARY. Restored files committed to git. The overwrite source (OttoGSD/src/orchestrator/journey-test-generator.ts) is identified and the restore-from-HEAD playbook is confirmed as the standard mitigation.</done>
</task>

</tasks>

<verification>
1. `git diff HEAD -- .auto-gsd/journey-tests/` — no differences (all 4 files match HEAD)
2. `cd .auto-gsd/journey-tests && npx playwright test` — 12 passed, 0 failed
3. `grep "SERVER_URL" .auto-gsd/journey-tests/journey-phase-14.spec.ts` — matches found (not BASE_URL stubs)
4. `grep "workers" .auto-gsd/journey-tests/playwright.config.ts` — workers: 1 present
5. Phase SUMMARY documents the root cause (OttoGSD journey-test-generator.ts)
</verification>

<success_criteria>
- All 12 journey tests pass green (4 build-and-start + 4 phase-14 + 4 phase-15)
- All 4 test files match their committed HEAD versions
- Root cause documented: OttoGSD/src/orchestrator/journey-test-generator.ts overwrites unconditionally after every phase
- Restored files committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/15.5-fix-journey-phase-14-schema-foundation-and-shared-types-completes-successfully/15.5-01-SUMMARY.md`
</output>
